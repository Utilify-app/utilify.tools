import React from 'react';

import PixelToRem from '.';

export default {
  title: 'Tools/PixelToRem',
  component: PixelToRem,
};

const Template = (args: any) => <PixelToRem {...args} />;
export const Default = Template.bind({});
